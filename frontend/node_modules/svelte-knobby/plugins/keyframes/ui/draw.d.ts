/**
 * @param {CanvasRenderingContext2D} ctx
 * @param {Record<string, import('../types').KeyframeTrack>} value
 * @param {string[]} active_tracks
 * @param {import('../types').Selection} selected
 * @param {{ x: (n: number) => number, y: (n: number) => number }} project
 * @param {{ x: (n: number) => number, y: (n: number) => number }} unproject
 * @param {{ x1: number, x2: number, y1: number, y2: number }} bounds
 * @param {number} playhead
 * @param {import('../types').Snap} snap
 */
export function draw(ctx: CanvasRenderingContext2D, value: Record<string, import('../types').KeyframeTrack>, active_tracks: string[], selected: import('../types').Selection, project: {
    x: (n: number) => number;
    y: (n: number) => number;
}, unproject: {
    x: (n: number) => number;
    y: (n: number) => number;
}, bounds: {
    x1: number;
    x2: number;
    y1: number;
    y2: number;
}, playhead: number, snap: import('../types').Snap): void;
